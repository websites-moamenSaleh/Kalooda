-- Placeholder: this version exists on production (Tokyo) but was missing from the repo.
-- If Tokyo applied DDL here, replace this file with that exact SQL (from Dashboard → Migrations or history).
-- Safe no-op for a fresh project (e.g. Frankfurt) until the real statements are recovered.

select 1;
